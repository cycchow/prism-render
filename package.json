{
  "version": "1.5.3",
  "scripts": {
    "start": "node prismrender.js",
    "tagAndPush": "docker buildx build --platform=linux/amd64 --push -t gcp-cr.dllm.me/hkjc-racingdata/prism-render:$npm_package_version . && git push origin :refs/tags/$npm_package_version && git tag -fa $npm_package_version -m \"for build $npm_package_version\" && git push origin HEAD --tags"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.5",
    "node-cache": "^5.1.2",
    "puppeteer": "^24.40.0"
  }
}
